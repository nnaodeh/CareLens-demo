# CareLens MVP

Static synthetic-data prototype for CareLens.

## Local use
Open `index.html` in a modern browser.

## GitHub Pages
Publish the repository root from the `main` branch.

## Netlify / Vercel
Deploy this folder directly.

Important: this prototype contains synthetic data only and is not suitable for real children's care records.
